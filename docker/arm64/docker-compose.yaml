version: "3"

services:
  app:
    image: skero2004/all_seaing_vehicle:arm64
    container_name: all_seaing_vehicle
    build: .
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
    network_mode: host
    stdin_open: true
    tty: true
